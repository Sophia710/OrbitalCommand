# 市场商机分析数字员工 · 实施计划

> 版本：v1.0  
> 对应需求：[市场商机分析数字员工需求文档.md](./市场商机分析数字员工需求文档.md)  
> 工程基线：`d:\XW\Project\Digital_Employee\frontend`（Vite 5 + Vue 3 + Element Plus SPA）  
> 关键约束：**所有 ChatOverlay 改造通过 `isMarketRadar` 条件分支隔离，零影响其他员工**

---

## 目录

1. [总览](#1-总览)
2. [阶段 A：数据层与 mock 引擎](#2-阶段-a数据层与-mock-引擎)
3. [阶段 B：ChatOverlay 隔离增强](#3-阶段-bchatoverlay-隔离增强)
4. [阶段 C：新增独立组件](#4-阶段-c新增独立组件)
5. [阶段 D：对话流与消息渲染](#5-阶段-d对话流与消息渲染)
6. [阶段 E：回归测试与验证](#6-阶段-e回归测试与验证)
7. [阶段 F：联调与内测](#7-阶段-f联调与内测)
8. [风险与回滚预案](#8-风险与回滚预案)
9. [附录：文件清单与依赖](#9-附录文件清单与依赖)

---

## 1. 总览

### 1.1 关键里程碑

| 里程碑 | 周期 | 交付物 | 验收 |
| --- | --- | --- | --- |
| **M0** 工程基线 | 0.5 天 | 改造前 5 个员工对话页面截图基线 | Playwright 截图归档 |
| **M1** 数据层就绪 | 1.0 天 | mock 引擎支持新员工 + 4 步 + 3 段 | mock 单元测试通过 |
| **M2** ChatOverlay 隔离改造 | 1.5 天 | 工具箱按钮 + 知识库绑定按钮 + v-if 隔离 | 回归测试 12 项全过 |
| **M3** 6 个新组件就绪 | 2.5 天 | 弹窗/抽屉/图表/列表/下载全部可用 | 单元测试 + 视觉走查 |
| **M4** 对话流端到端 | 1.0 天 | 完整任务 4 步 → 3 段输出可演示 | 端到端冒烟 |
| **M5** 回归与上线** | 1.0 天 | `__test_regression.js` 全过 + 截图 diff < 0.5% | 验收清单全过 |
| **合计** | **~7.5 天** | **MVP 可上线** | |

### 1.2 任务依赖图

```
M0 截图基线 ─┐
              ├─→ M1 数据层 ─┐
M0 截图基线 ─┘               │
                             ├─→ M2 ChatOverlay 隔离 → M3 组件 → M4 端到端 → M5 回归
                             │
              知识库 API ────┘
```

### 1.3 实施原则

- **每个任务都有 DoD**（Definition of Done）：明确产出与验收
- **每个任务都可独立 PR**：避免巨型 commit
- **每阶段结束做回归**：保证不污染其他员工
- **mock 优先**：所有数据走 `mock.js`，不直连真实后端

---

## 2. 阶段 A：数据层与 mock 引擎

### 任务 A.1：员工数据注入

**目标**：在 `src/api/mock-data.js` 中追加"市场商机分析员"员工数据

**产出文件**：
- 修改 [mock-data.js](file:///d:/XW/Project/Digital_Employee/frontend/src/api/mock-data.js) — 在 `EMPLOYEES` 数组追加
- 修改 [employees.js](file:///d:/XW/Project/Digital_Employee/frontend/src/api/employees.js) — 无需改（自动从 mock-data 读）

**任务步骤**：
1. 在 `EMPLOYEES` 数组末尾追加：
   ```js
   {
     id: 'market-radar-001',
     name: '市场商机分析员',
     series: 'professional',           // 专业员工
     kind: 'professional',
     category: '市场情报',
     tier: 99,                          // 置底
     avatar: '🔭',
     description: '7×24 监控全网商机信号，基于公司知识库研判，输出可执行的情报建议',
     tags: ['客户洞察', '标讯跟踪', '竞品分析', '政策解读', '行业动态'],
     defaultQuery: '用自然语言描述您想查询的标讯信息，如：帮我找近期遥感领域的招标公告',
   }
   ```
2. 确认 `Plaza.vue` 中"专业员工"分类自动出现新卡片
3. 确认 `MyEmployees.vue` "添加到我的员工"流程可用

**DoD**：
- ✅ Plaza 页面"专业员工"分类下能看到"市场商机分析员"
- ✅ 点击"添加到我的员工"成功
- ✅ MyEmployees 列表显示该员工
- ✅ 点击"发布任务"打开 ChatOverlay，员工名显示"市场商机分析员"
- ✅ 其他 5+ 个员工数据**完全不变**

**工时**：0.5 天

---

### 任务 A.2：mock 引擎扩展（4 步 + 3 段）

**目标**：扩展 [mock.js](file:///d:/XW/Project/Digital_Employee/frontend/src/api/mock.js) 增加市场商机分析员的消息流生成器

**产出文件**：
- 修改 [mock.js](file:///d:/XW/Project/Digital_Employee/frontend/src/api/mock.js) — 新增 `buildMarketRadarMessages(query)` 函数
- 修改 [chat.js](file:///d:/XW/Project/Digital_Employee/frontend/src/stores/chat.js) — 在 `openChat` 中按 employee.id 分支

**任务步骤**：
1. 在 `mock.js` 中实现 `buildMarketRadarMessages(query)`：
   - 返回 4 步 `process_card`（4 steps，初始 status='pending'）
   - 延迟 200ms 依次更新 step status（pending → running → success）
   - 4 步完成后输出：
     - `chart` 消息 × 3（饼图/柱图/雷达图）
     - `result_list` 消息（10 条样例）
     - `markdown` 消息（深度分析报告，含 download actions）
2. 在 `chat.js` 的 `openChat` 中：
   ```js
   openChat(emp) {
     this.employee = emp
     if (emp?.id === 'market-radar-001') {
       this.messages = buildInitialMessages(emp)
       this.messages.push(...buildMarketRadarMessages(emp.defaultQuery))
       // 流式触发 4 步更新
     } else {
       this.messages = [...buildInitialMessages(emp), ...MOCK_DIALOG]
     }
     this.open = true
   }
   ```
3. **关键**：其他员工的 `MOCK_DIALOG` 逻辑**完全不变**

**DoD**：
- ✅ 发送任意 query 后，4 步 process_card 依次从 pending → success
- ✅ 4 步完成后输出 3 段（3 chart + 1 result_list + 1 markdown）
- ✅ 链路诊断员等其他员工**完全不受影响**（分支外逻辑不动）

**工时**：0.5 天

---

## 3. 阶段 B：ChatOverlay 隔离增强

### 任务 B.1：isMarketRadar 计算属性

**目标**：在 [ChatOverlay.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/components/ChatOverlay.vue) 顶部建立唯一识别标志

**任务步骤**：
1. 在 `<script setup>` 中：
   ```js
   const props = defineProps({ employee: Object, open: Boolean })
   const isMarketRadar = computed(() => props.employee?.id === 'market-radar-001')
   ```
2. **不要**修改现有 prop 接口、不修改现有 ref、不修改现有 emit
3. **不要**给员工添加全局新字段（如 `enableToolbox: true`）

**DoD**：
- ✅ 计算属性 `isMarketRadar` 在 `employee.id === 'market-radar-001'` 时为 true
- ✅ 其他任何员工时为 false
- ✅ 不影响现有 prop / ref / emit

**工时**：0.1 天

---

### 任务 B.2：侧栏工具箱按钮 + 折叠面板

**目标**：在 ChatOverlay 侧栏头部（"历史任务"标题上方）新增"🧰 工具箱"按钮

**任务步骤**：
1. 定位现有侧栏头部 DOM 结构（在历史任务标题之前）
2. 在该位置插入：
   ```vue
   <button v-if="isMarketRadar" class="toolbox-button" @click="toggleToolbox">
     🧰 工具箱 <span :class="toolboxOpen ? 'caret-up' : 'caret-down'"></span>
   </button>
   <div v-if="isMarketRadar && toolboxOpen" class="toolbox-panel">
     <button class="toolbox-entry" @click="openDataSourceDialog">
       📡 数据源设置 <span>→</span>
     </button>
     <button class="toolbox-entry" @click="openPushDialog">
       🔔 消息推送 <span>→</span>
     </button>
   </div>
   ```
3. 在 `<script setup>` 新增：
   ```js
   const toolboxOpen = ref(false)
   const toggleToolbox = () => { toolboxOpen.value = !toolboxOpen.value }
   const openDataSourceDialog = () => { /* 触发 DataSourceDialog */ }
   const openPushDialog = () => { /* 触发 PushDialog */ }
   ```
4. CSS 沿用 Element Plus 主题色，不引入新视觉风格

**DoD**：
- ✅ 市场商机分析员对话侧栏顶部显示"🧰 工具箱"按钮
- ✅ 点击展开后显示两个入口按钮
- ✅ 点击"数据源设置"打开 DataSourceDialog
- ✅ 点击"消息推送"打开 PushDialog
- ✅ 其他员工**完全看不到**该按钮（DOM 中不存在 `.toolbox-button`）
- ✅ 切换员工后状态正确清理

**工时**：0.4 天

---

### 任务 B.3：输入框旁📎绑定知识库按钮

**目标**：在 ChatOverlay 输入框内（textarea 旁）新增"📎 绑定知识库"按钮

**任务步骤**：
1. 定位现有输入框 DOM 结构
2. 在 textarea 旁（**不破坏现有布局**）插入：
   ```vue
   <button v-if="isMarketRadar" class="bind-kb-button" @click="openKnowledgeDrawer">
     📎 绑定知识库
     <span v-if="boundDocCount" class="bound-badge">已绑定 {{ boundDocCount }} 篇</span>
   </button>
   ```
3. 在 `<script setup>` 新增：
   ```js
   const boundDocCount = computed(() => chatStore.boundDocs?.length || 0)
   const openKnowledgeDrawer = () => { /* 触发 KnowledgeBindDrawer */ }
   ```
4. 调整 textarea 宽度，**不破坏现有响应式布局**

**DoD**：
- ✅ 市场商机分析员输入框旁显示"📎 绑定知识库"
- ✅ 已绑定时显示"已绑定 N 篇"徽标
- ✅ 点击打开 KnowledgeBindDrawer
- ✅ 其他员工**完全看不到**（DOM 中不存在 `.bind-kb-button`）
- ✅ 现有输入框布局不变（其他员工无影响）

**工时**：0.3 天

---

### 任务 B.4：基础状态管理（toolboxOpen / dialogs / drawer）

**目标**：为 B.2 / B.3 配套的状态管理

**任务步骤**：
1. 在 `chat.js` store 中新增：
   ```js
   state: {
     // ... 现有
     toolboxOpen: false,
     dataSourceDialogOpen: false,
     pushDialogOpen: false,
     knowledgeDrawerOpen: false,
     boundDocs: [],  // 当前会话绑定的知识库
   },
   actions: {
     openDataSourceDialog() { this.dataSourceDialogOpen = true },
     closeDataSourceDialog() { this.dataSourceDialogOpen = false },
     // 同 push / knowledge
     bindDocs(docIds) { this.boundDocs = docIds },
   }
   ```
2. **不修改**现有 state 字段、不修改现有 actions 签名

**DoD**：
- ✅ store 新增字段不破坏现有逻辑
- ✅ 其他员工使用时 `boundDocs` 永远为空数组（`v-if="isMarketRadar"` 隔离）

**工时**：0.2 天

---

### 任务 B.5：现有 4 类消息渲染逻辑保持不变

**目标**：在改造过程中**绝不修改**现有 4 类消息的渲染逻辑

**约束**：
- `process_card` / `step` / `markdown` / 普通消息的渲染**一行不动**
- 只**新增** `chart` / `result_list` / `download_bar` 三个分支
- 在 MessageRenderer 中**先保留**现有 v-if 分支，再在末尾追加新分支

**DoD**：
- ✅ 现有 4 类消息渲染代码 100% 保持
- ✅ 视觉对比（其他员工）与改造前像素级一致

**工时**：0（约束，非任务）

---

## 4. 阶段 C：新增独立组件

### 任务 C.1：DataSourceDialog（数据源设置弹窗）

**目标**：实现工具箱"数据源设置"入口的弹窗

**产出文件**：
- 新建 [DataSourceDialog.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/components/tools/DataSourceDialog.vue)

**任务步骤**：
1. 弹窗结构（Element Plus `el-dialog`，720px）：
   - 顶部操作区：`[+ 新增数据源] [⟳ 全部同步]`
   - 数据源列表（每条：名称/类型/URL/频率/状态/操作）
   - 底部：`[取消] [保存]`
2. "新增数据源"内嵌表单：
   - 字段：数据源名称、类型、URL、频率、启用状态
3. 列表操作：编辑、测试、暂停/启用、删除
4. 数据持久化：`localStorage` key = `marketRadar.datasources.{userId}`
5. mock API：`tools.saveDataSources(payload)` 在 [mock.js](file:///d:/XW/Project/Digital_Employee/frontend/src/api/mock.js) 中实现（400ms 延迟）

**DoD**：
- ✅ 点击"数据源设置"打开弹窗
- ✅ 列表展示 mock 预置 3-5 条样例
- ✅ 新增/编辑/删除/暂停/启用操作可用
- ✅ 保存后写入 localStorage，刷新后保留
- ✅ 仅在 isMarketRadar 时被引入

**工时**：0.6 天

---

### 任务 C.2：PushDialog（消息推送设置弹窗）

**目标**：实现工具箱"消息推送"入口的弹窗

**产出文件**：
- 新建 [PushDialog.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/components/tools/PushDialog.vue)

**任务步骤**：
1. 弹窗结构（Element Plus `el-dialog`，640px）：
   - 基础规则区：订阅类型、订阅方向、关键词、区域范围、数据源、更新方式
   - 推送渠道区：站内/邮件/企业IM 复选框 + 邮箱 + 时段
   - 研判规则区：6 项复选框
   - 底部：`[预览] [测试推送] [取消] [保存]`
2. 字段对齐市场雷达原型（订阅类型、更新方式、订阅对象/方向、关键词、区域、数据源）
3. 订阅类型下拉：标讯/客户/伙伴/竞品/政策/行业
4. 数据持久化：`localStorage` key = `marketRadar.push.{userId}`

**DoD**：
- ✅ 点击"消息推送"打开弹窗
- ✅ 全部字段可填写
- ✅ 保存后写入 localStorage
- ✅ 仅在 isMarketRadar 时被引入

**工时**：0.6 天

---

### 任务 C.3：KnowledgeBindDrawer（知识库绑定抽屉）

**目标**：实现"📎 绑定知识库"按钮的抽屉

**产出文件**：
- 新建 [KnowledgeBindDrawer.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/components/tools/KnowledgeBindDrawer.vue)
- 新建或复用 [KbDocuments.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/views/KbDocuments.vue) 的"列出可绑定资料"API

**任务步骤**：
1. 抽屉结构（Element Plus `el-drawer`，右侧 380px）：
   - 顶部：`📚 绑定知识库 [✕]`
   - 搜索框
   - 可勾选的知识库列表
   - 底部：`已选 N/总数 [取消] [确定]`
2. mock 知识库数据注入 6-8 条（遥感产品手册、案例、资质等）
3. KbDocuments 提供 `listBindableDocs()` mock API（无则新建）
4. 确定后调用 `chat.bindDocs(sessionId, docIds)`

**DoD**：
- ✅ 点击"📎 绑定知识库"打开抽屉
- ✅ 列表展示 6-8 条可绑定知识库
- ✅ 搜索框可过滤
- ✅ 确定后输入框显示"已绑定 N 篇"
- ✅ 仅在 isMarketRadar 时被引入

**工时**：0.5 天

---

### 任务 C.4：MarketChart（统一图表组件）

**目标**：支持饼图/柱图/雷达图三种图表的统一组件

**产出文件**：
- 新建 [MarketChart.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/components/charts/MarketChart.vue)
- 安装依赖：`echarts`（推荐）或 `chart.js`

**任务步骤**：
1. 组件 props：`chartType`（pie/bar/radar）、`title`、`data`、`direction`、`dimensions`、`scores`
2. 三种图表渲染（用 ECharts 简洁版）
3. 右上角"实时渲染组件"按钮（折叠/展开）—— 沿用参考图
4. 样式：沿用 Element Plus 主题色（蓝/青/紫/橙/红）

**DoD**：
- ✅ 三种图表正确渲染
- ✅ 标题/数据/图例完整
- ✅ 折叠/展开可用
- ✅ 仅在 isMarketRadar 时被引入

**工时**：0.5 天

---

### 任务 C.5：ResultList（检索结果列表组件）

**目标**：渲染前 10 条检索结果

**产出文件**：
- 新建 [ResultList.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/components/charts/ResultList.vue)

**任务步骤**：
1. 列表 props：`items`、`total`、`highCount`、`midCount`
2. 每条结构：序号（圆形数字标签）+ 标题（粗体）+ "中标1次" 标签 + 元数据（来源/费用/时间）+ 摘要
3. 列表尾部 `共 N 条结果，本页展示前 M 条`
4. 样式：沿用 Element Plus 主题色

**DoD**：
- ✅ 正确渲染 10 条样例数据
- ✅ 序号/标签/元数据/摘要完整
- ✅ 仅在 isMarketRadar 时被引入

**工时**：0.3 天

---

### 任务 C.6：DownloadBar（Word/Excel 下载按钮组）

**目标**：在 markdown 报告底部渲染下载按钮组

**产出文件**：
- 新建 [DownloadBar.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/components/tools/DownloadBar.vue)
- 安装依赖：`html-docx-js`（Word）、`xlsx` (SheetJS)（Excel）

**任务步骤**：
1. 组件 props：`actions`
2. 按钮组：`[📥 下载 Word] [📥 下载 Excel]`
3. Word：把 markdown 转 .docx
4. Excel：把 `result_list` 数据转 .xlsx
5. 文件名：`市场商机分析-{任务标题前16字}-{YYYYMMDD-HHmm}.{ext}`

**DoD**：
- ✅ 点击 Word 下载 .docx 文件
- ✅ 点击 Excel 下载 .xlsx 文件
- ✅ 文件名规范
- ✅ 仅在 isMarketRadar 时被引入

**工时**：0.5 天

---

### 任务 C.7：KnowledgeRef（知识库引用上标）

**目标**：在 markdown 报告中渲染 `[1][2]` 上标引用

**产出文件**：
- 新建 [KnowledgeRef.vue](file:///d:/XW/Project/Digital_Employee/frontend/src/components/tools/KnowledgeRef.vue)
- 或：扩展现有 markdown 渲染组件

**任务步骤**：
1. 解析 markdown 中的 `[n]` 模式
2. 渲染为上标 `<sup>[n]</sup>`
3. 悬停 tooltip 显示资料名+版本
4. 仅当 `boundDocs.length > 0` 时显示

**DoD**：
- ✅ 报告中正确显示上标
- ✅ 悬停显示资料详情
- ✅ 无绑定时不上标
- ✅ 仅在 isMarketRadar 时被引入

**工时**：0.2 天

---

## 5. 阶段 D：对话流与消息渲染

### 任务 D.1：MessageRenderer 类型分发

**目标**：在 MessageRenderer 中按 `msg.type` 分发到对应渲染组件

**任务步骤**：
1. 在现有 MessageRenderer 模板的**末尾**追加：
   ```vue
   <template>
     <!-- 现有 4 类：完全保留 -->
     <ProcessCard v-else-if="msg.type === 'process_card'" :msg="msg" />
     <StepBlock  v-else-if="msg.type === 'step'" :msg="msg" />
     <Markdown   v-else-if="msg.type === 'markdown'" :msg="msg" />
     <DefaultBubble v-else :msg="msg" />

     <!-- 新增：仅 isMarketRadar -->
     <MarketChart v-else-if="msg.type === 'chart' && isMarketRadar" :msg="msg" />
     <ResultList v-else-if="msg.type === 'result_list' && isMarketRadar" :msg="msg" />
     <DownloadBar v-else-if="msg.actions?.length && isMarketRadar" :msg="msg" />
   </template>
   ```
2. **必须**把现有 4 个 v-else-if 完整保留
3. `isMarketRadar` 通过 `props.employee` 计算

**DoD**：
- ✅ 现有 4 类消息渲染逻辑 0 修改
- ✅ 新增 3 类仅在 isMarketRadar 时渲染
- ✅ 其他员工不会渲染新组件

**工时**：0.3 天

---

### 任务 D.2：mock 输出对齐参考图

**目标**：mock 引擎输出的图表数据、检索结果、markdown 报告与参考图一致

**任务步骤**：
1. 在 `mock.js` 中实现 `buildMarketRadarMessages` 完整逻辑：
   - chart1（pie）：数据源构成
   - chart2（bar）：区域标讯分布 Top
   - chart3（radar）：市场吸引力六维评估
   - chart4（bar）：项目金额区间分布
   - result_list：10 条样例（按参考图）
   - markdown：7 章节深度分析报告（检索结果概述/市场格局/高价值商机/时间窗口/风险与壁垒/关键数据/行动建议/投标策略）
2. 数据来源：参考上传图片中的实际问答示例
3. **不修改**其他员工的 mock 流程

**DoD**：
- ✅ 4 个图表、10 条结果、深度分析报告完整
- ✅ 文本内容与参考图示例一致
- ✅ 其他员工 mock 完全不变

**工时**：0.5 天

---

## 6. 阶段 E：回归测试与验证

### 任务 E.1：__test_regression.js 员工遍历

**目标**：自动验证所有非市场商机分析员员工**不出现**新功能元素

**任务步骤**：
1. 在 [__test_regression.js](file:///d:/XW/Project/Digital_Employee/frontend/__test_regression.js) 追加：
   ```js
   // 回归测试：所有非市场商机分析员员工不应看到工具箱
   const emps = listEmployees().filter(e => e.id !== 'market-radar-001')
   emps.forEach(emp => {
     chat.openChat(emp)
     expect(document.querySelector('.toolbox-button')).toBeNull()
     expect(document.querySelector('.bind-kb-button')).toBeNull()
     expect(document.querySelector('.toolbox-panel')).toBeNull()
     chat.closeChat()
   })
   ```
2. 执行 `node __test_regression.js`，所有员工通过

**DoD**：
- ✅ 5+ 个员工全部通过回归测试
- ✅ 无控制台报错

**工时**：0.3 天

---

### 任务 E.2：Playwright 截图基线对比

**目标**：用 Playwright 截图 5 个员工的对话页面，改造前后做 diff

**任务步骤**：
1. 改造前：截图 5 个员工对话页面（链路诊断员/全链路编排员/终端管理助理/报告生成员/数据处理员）作为基线
2. 改造后：再次截图
3. 用 Playwright `toHaveScreenshot` 或 pixelmatch 做 diff
4. 差异 > 0.5% 视为回归失败

**DoD**：
- ✅ 5 个员工页面改造前后像素级一致（diff < 0.5%）

**工时**：0.4 天

---

### 任务 E.3：手动回归清单（12 项）

**目标**：人工验证 9.4 节 12 项验收项

**任务步骤**：
1. 进入 5 个员工的对话
2. 逐项核对：
   - 侧栏无🧰工具箱按钮
   - 输入框无📎绑定知识库按钮
   - 工具箱面板无渲染
   - process_card / step / markdown 渲染与改造前一致
   - 历史对话消息流一致
   - 切换员工后无残留 DOM

**DoD**：
- ✅ 12 项全部通过

**工时**：0.3 天

---

## 7. 阶段 F：联调与内测

### 任务 F.1：端到端联调

**目标**：完整走通"市场商机分析员"对话链路

**任务步骤**：
1. Plaza 添加员工
2. MyEmployees 找到该员工
3. 点击"发布任务"
4. 发送查询："找遥感、卫星相关的招标商机，时间范围：近一个月"
5. 验证：
   - 4 步 process_card 依次执行
   - 右侧进程面板同步
   - 3 段输出（4 图表 + 10 条结果 + 7 章节报告）完整
   - 知识库引用上标可用
   - Word/Excel 下载成功
   - 工具箱数据源/推送设置可保存

**DoD**：
- ✅ 端到端完整链路通畅
- ✅ 所有功能可用

**工时**：0.4 天

---

### 任务 F.2：内部试用（3-5 人 × 1 周）

**目标**：邀请内部市场/销售人员试用 1 周，收集反馈

**任务步骤**：
1. 招募 3-5 名试用人员
2. 发放试用指引
3. 收集反馈（工具箱配置流程、新功能易用性、图表清晰度、报告完整性等）
4. 整理优化清单

**DoD**：
- ✅ 收集 3-5 人反馈
- ✅ 输出优化清单

**工时**：0.5 天（不含 1 周等待期）

---

## 8. 风险与回滚预案

| 风险 | 等级 | 应对 | 回滚 |
| --- | --- | --- | --- |
| ChatOverlay 改造误改其他员工逻辑 | 高 | 严格 v-if 隔离 + 每步回归 | Git revert 至上一 commit |
| ECharts 体积过大 | 中 | 按需引入 + tree-shaking | 切回 chart.js 或纯 CSS 简易图表 |
| 知识库 API 改造影响现有知识库模块 | 中 | KbDocuments 现有逻辑保持，新增 listBindableDocs | 移除新 API |
| 弹窗样式与系统不一致 | 中 | 严格沿用 Element Plus 主题色 | CSS 调整 |
| mock 数据不真实 | 低 | 用参考图样例填充 | 更新 mock.js |

**回滚策略**：
- 阶段 A/B/C/D 任一 PR 失败 → revert 该 PR，不影响主线
- 阶段 E 回归测试失败 → 立即停止，进入修复
- 上线后 1 周内有 3+ 用户反馈 → 临时下线，回滚到 M0 基线

---

## 9. 附录：文件清单与依赖

### 9.1 新增文件（4 个）

| 文件路径 | 用途 | 阶段 |
| --- | --- | --- |
| `src/components/charts/MarketChart.vue` | 统一图表组件（饼/柱/雷达） | C.4 |
| `src/components/charts/ResultList.vue` | 检索结果列表 | C.5 |
| `src/components/tools/DataSourceDialog.vue` | 数据源设置弹窗 | C.1 |
| `src/components/tools/PushDialog.vue` | 消息推送设置弹窗 | C.2 |
| `src/components/tools/KnowledgeBindDrawer.vue` | 知识库绑定抽屉 | C.3 |
| `src/components/tools/DownloadBar.vue` | 下载按钮组 | C.6 |
| `src/components/tools/KnowledgeRef.vue` | 知识库引用上标 | C.7 |

### 9.2 修改文件（5 个）

| 文件路径 | 改造内容 | 阶段 |
| --- | --- | --- |
| `src/api/mock-data.js` | 追加"市场商机分析员"员工 | A.1 |
| `src/api/mock.js` | 新增 `buildMarketRadarMessages` | A.2 / D.2 |
| `src/stores/chat.js` | `openChat` 按 emp.id 分支 + 新增 state | A.2 / B.4 |
| `src/components/ChatOverlay.vue` | 工具箱按钮 + 知识库绑定按钮 + v-if 隔离 | B.2 / B.3 |
| `src/views/KbDocuments.vue` | 新增 `listBindableDocs()` API | C.3 |

### 9.3 测试文件

| 文件路径 | 用途 | 阶段 |
| --- | --- | --- |
| `__test_regression.js` | 员工遍历回归测试 | E.1 |
| `__test_market_radar_e2e.js` | 端到端冒烟 | F.1 |
| Playwright 截图基线 | 像素级 diff | E.2 |

### 9.4 新增依赖

| 包名 | 用途 | 体积影响 |
| --- | --- | --- |
| `echarts` | 图表渲染 | ~900KB（可按需引入压到 300KB） |
| `html-docx-js` | Word 下载 | ~200KB |
| `xlsx` (SheetJS) | Excel 下载 | ~400KB |

**优化**：可使用 ECharts 按需引入（仅 pie/bar/radar）压到 ~300KB。

### 9.5 localStorage 键

| 键名 | 用途 | 大小 |
| --- | --- | --- |
| `marketRadar.datasources.{userId}` | 数据源配置 | < 5KB |
| `marketRadar.push.{userId}` | 推送配置 | < 2KB |

---

## 实施准备清单

- [ ] 确认 ECharts vs chart.js 选型
- [ ] 确认 docx.js vs html-docx-js 选型
- [ ] 确认 KbDocuments 现有 API 复用方案
- [ ] 确认 Element Plus 版本兼容性
- [ ] 确认 localStorage 键名与项目现有命名空间不冲突

---

> 实施计划结束 · 7.5 天 MVP 路径 · 任一阶段失败可独立回滚
